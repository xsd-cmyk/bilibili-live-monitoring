# 项目总结：主播监控（BiliMonitor）

> 一份写给「想读代码的人」和「未来的自己」的总结：**这个项目做了什么、怎么做的、值得看什么**。
> 所有「踩过的坑」都来自项目自己的缺陷台账 [`docs/DEFECT_LEDGER.md`](docs/DEFECT_LEDGER.md)（H1–H55）；
> 每条都标明了它是**实测复现**、**静态审查确认**，还是**仅静态推断 / 仅编译验证** ——
> 下面引用时也按同样的口径：编译通过 ≠ 功能可用。

- 规模：`app/src/main/java` **92 个 Kotlin 文件 / 约 2.4 万行**；`@Entity` 44 个、`@Query` 244 处
- 测试：单元测试 **11 个文件 / 90 个用例**；instrumented 测试 1 个用例
- 平台：Android，minSdk 31 / targetSdk 35；Kotlin 2.0.21 + Compose（BOM 2024.12.01）+ Room 2.6.1 + Hilt 2.52 + WorkManager 2.10.0
- 数据库：`DB_VERSION = 8`，迁移链 1→8（7 个 `Migration` 对象），`app/schemas/` 保留 v2–v8 的 schema JSON
- 版本：`versionName 1.1` / `versionCode 2`

---

## 一、项目定位与设计原则

**定位**：一个本地优先、**可核查**的 B 站主播开播监控应用。它追求的不是「通知最快」，
而是「记录可信」—— 每一次开播/关播都要有来源、有可信度、有失败的痕迹；
数据缺了就承认缺了（盲区），而不是补一条看起来完整的假记录。

支撑这一点的是下面这些**在代码里被反复执行**的原则：

| 原则 | 具体表现 |
|---|---|
| **任何降级都不允许静默发生** | 失败要么重试、要么留痕（错误日志 / 健康事件 / 盲区记录）。曾有一批 `runCatching` 直接吞异常导致「清理链从未执行」「服务假活」，现在要求失败必须可查 |
| **界面不得显示「用户选择所暗示的乐观状态」** | 选了「无障碍 + 前台通知双保险」但无障碍没开时，横幅显示的是降级文案，而不是「监控中」 |
| **区分「假成功」与「假失败」** | 文件已写出却因事后记账失败而报「导出失败」，会让用户反复重试、产生一堆副本 → 产物的生成结果与记账结果分开判定 |
| **DB / API 出来的字符串一律视为不可信数据** | 枚举列读路径对未知值退化为最保守取值并留痕；B 站 DTO 字段缺失/越界不当作合法值（`live_status` 缺失曾被当成「未开播」，足以让全部主播被确认下播） |
| **判定语义与显示语义分离** | 轮播按「未开播」处理（要关场次、要发下播通知），但状态仍记为「轮播」，历史与界面看得出它是轮播 |
| **单一数据源 + 对外格式长期兼容** | 业务数据只落 SQLite，界面全部由 `Flow` 驱动；配置备份的校验和按**文件里的原始文本**计算，因此给备份 schema 加字段不会让历史备份全部失配 |
| **线程归属由仓库层负责** | 仓库的 suspend 函数自己决定跑在 IO 还是 Default，调用方不必关心，也不允许整文件 IO 落在主线程 |
| **修复本身也要被审查** | 第二轮审查抓到的最有价值问题，几乎全是第一轮修复「改了一半」或「引入的回归」 |
| **记录「未验证」** | 编译通过 ≠ 功能可用。台账对每条都标注了验证状态，「未在设备验证」的部分不会被写成功效 |

---

## 二、架构分层与关键机制

### 2.1 分层

| 层 | 职责 | 关键约束 |
|---|---|---|
| `ui/` | Compose 界面 + ViewModel（单向数据流） | 不直接写库；对话框状态提到 Screen 顶层（`LazyColumn` item 里的 `remember` 会被回收） |
| `domain/` | 纯函数策略：状态确认、重试/退避/熔断、免打扰 | 「判断」与「写库」分离，才能不启动模拟器就测边界；纯函数与纯对象是单测的主战场（见 4.1） |
| `data/repository/` | 业务仓储：监控提交、历史、统计、导入导出、备份恢复、外观设置 | 负责事务边界与线程归属 |
| `data/local/` | Room 实体 / DAO / 迁移 / 类型转换器 | 部分唯一索引在**数据库层**兜底业务约束 |
| `background/` | 监控引擎、前台服务、WorkManager、启动恢复、通知投递、无障碍保活 | 唯一写入者语义由租约 + 代际 + CAS 保证 |
| `core/` | 双时钟、崩溃记录、凭证存储、WBI 签名 | 不依赖业务层 |
| `notify/` | 通知渠道、通知渲染、URL 白名单、直播间跳板 Activity | 通知内容必须过 URL 白名单，绝不把库里的任意字符串交给系统打开 |

### 2.2 双时钟

时间判定分三类，**不允许混用**：

| 用途 | 时钟 | 原因 |
|---|---|---|
| 展示、「多久以前」、跨重启比较 | `nowWall()` = 单调时钟 + 网络校时偏移 | 用户改系统时间/时区也不会让历史错乱 |
| 租约、聚合窗口、退避 | `nowElapsed()` | 单调递增，不受校时影响 |
| 跨开机判定 | `bootId()`（开机计数，读不到时用开机时刻锚点，且**缓存**） | `elapsedRealtime` 开机会归零，跨 boot 比较毫无意义 |

跨 boot 的窗口/租约一律退回墙上时间比较，或直接作废。

### 2.3 写入纪律：一个状态转换 = 一个事务 + 租约 + 围栏令牌（fencing）+ CAS

每次状态提交都走同一个入口，在一个事务里完成：状态 CAS → 盲区/区间维护 → 状态历史 → 事件 →
场次 → 监测区间 → 待确认复位 → 通知入队 → 数据版本号递增。

- **CAS**：`observationSequence` + 主播代际 + 全局代际三者同时匹配才允许写入；
- **围栏令牌**：租约被抢占后，旧持有者的所有写入影响 0 行；
- **维护模式**：导出/恢复/清理期间 `maintenanceMode != 'OFF'`，CAS 同样返回 0 行；
- **主动接管**：用户发起的导出/备份/清理/恢复用「接管」版维护权，安全性由 fencing 保证，
  而不是靠「等租约过期」—— 后者曾让容量清理与保留策略**在活跃用户上从未执行过**；
- **自愈**：进程在维护中途被杀会留下永久维护标记（监控会永久停摆），冷启动时有 `resetStaleMaintenance` 清理并写健康事件。

**踩过的深坑**：`withTransaction` 里的 `return@withTransaction` 是**提交**而不是回滚。
早期捕获「重复建场次」信号后正常返回，等于提交了半截事务（状态已 LIVE、场次没建，
下一轮不再产生状态转换）→ **这段直播在历史/统计/导出里永久缺失**。
现在能就地挽救的（并发建场次冲突）改为「重读复用」，挽救不了的一律让异常穿透事务由 Room 回滚。

### 2.4 通知外发状态机与幂等 `eventKey`

通知**不是直接 post**，而是先落 `notification_outbox`，由投递器认领：

`PENDING → PROCESSING → SENT / RETRY_WAIT / FAILED / EXPIRED / DELIVERY_UNKNOWN`

| 机制 | 做法 |
|---|---|
| 幂等 | `eventKey`（`evt:{eventId}` / `agg:{aggregateId}` / `problem:{problemKey}` / `change:标题\|分区:主播:场次:新值`）+ `notificationId` 注册表：同一事件重复投递只会覆盖同一条系统通知 |
| 租约 | 认领时写 30 秒租约，过期由回收器结算；回收原先只在冷启动/省电 Worker 执行，实时模式反而取消了那个 Worker → 进程存活期间一次中断等于**通知永久丢失**，现已加入运行期回收 |
| 重试上限 | 持续抛异常的投递行不会以 30 秒间隔一路重试到 24h TTL（曾约 480 次/天 + 480 条日志），现在有明确的 `MAX_ATTEMPTS` 上限 |
| 聚合 | 多主播同时开播合成一条，窗口带 bootId 与单调时钟到期时间 |
| 免打扰 | **只抑制通知**，状态与历史照常记录；免打扰读取失败不会静默变成「关闭」（否则用户以为夜里不会被打扰，实际会被吵醒） |
| 投递隔离 | 投递移出监控互斥锁，封面专用客户端 + 5 秒超时、整体 15 秒预算（否则一次投递能把整轮监控堵住几分钟） |
| 清理 | 保留策略覆盖 outbox / 历史 / 投递尝试 / 聚合 / 各类日志；`notificationId` 注册表**刻意不清理**（稳定性依赖它） |

「系统问题」与「恢复」通知共用 `problem:` 前缀，曾因前缀判断抢在 `:recovered` 后缀之前命中，
把恢复通知一直显示成「系统问题通知」。

### 2.5 Room：迁移链、部分唯一索引、容错枚举

- **迁移链 1→8**：每步一个 `Migration` 对象；`DB_VERSION` 是**唯一来源**，
  `@Database(version = DB_VERSION)`、诊断包里的版本号全部引用它 ——
  此前版本号存在多个手写副本，导致备份清单双向判错、守卫形同虚设。
- **部分唯一索引**（带 `WHERE` 的唯一索引，如「同一主播只能有一条 ACTIVE 未结束场次」）：
  迁移时先 `DROP`、迁移后由 `onOpen` 幂等重建。两个教训：
  ① `CREATE UNIQUE INDEX IF NOT EXISTS` 在索引不存在时其实是**对既有数据的唯一性校验**，
  库里一旦有违规数据就会让 `onOpen` 抛异常 → **永久打不开数据库**，因此 `onOpen` 里绝不抛异常，
  16 条索引**逐条独立保护**（一条失败不连带跳过其余，否则唯一性静默失效），失败索引名上报健康事件；
  ② 索引不是万能的：`WHERE finishedAt IS NULL` 的唯一索引在 SQLite 里没有约束力（NULL 互不相同）。
- **容错枚举**：38 个类型转换器原本裸 `valueOf`，读到不认识的字符串就崩在 Room 读取路径上
  （实测：手工把 `lifecycleState` 写成 `'CLOSED'` → 启动即崩且反复崩，用户唯一自救手段是清数据）。
  现在统一为「未知值 → 语义最保守的取值 + 攒起来落 `application_error_log`」，并有 4 个单测钉住行为。
  同时确立约定：**枚举列新增常量必须同时升 `DB_VERSION`**（宽容读不是版本守卫的替代品）。
- **删除顺序无关**：外键是 `NO ACTION` 的表，靠「先删父再删子」的直觉曾让清理抛
  `FOREIGN KEY constraint failed`（异常被吞 → 通知历史与聚合清理从未执行，Outbox 无界增长）。
  现在把「无子行」写成删除条件本身（`NOT EXISTS` 子查询），与顺序、批次大小无关。
- **降级即崩溃**：`versionCode` 长期不递增 + 库版本回退过，导致装过中间版的用户升级后
  `A migration from 6 to 5 was required but not found` → 启动即崩且无法自愈。
  现已正常化版本（DB 8 / versionCode 2），诊断包也声明 `destructiveMigrationAllowed = false`。

### 2.6 后台保活：前台服务 + WorkManager + 无障碍，且**如实呈现**

- **前台服务类型用 `specialUse`**：`dataSync` 在 Android 15+ 有「24 小时内累计 6 小时」上限，
  到点系统强制停止，与「长期监控」需求根本冲突。
- **开机恢复必须在广播接收器里发起**：Android 12+ 禁止后台启动前台服务，豁免绑定「在广播接收器里发起」。
  曾把 FGS 启动挪进 WorkManager 的 Worker，结果**开机后再也不会起实时监控**（静默降级成 15 分钟周期）——
  这是「修复引入的回归」，靠第二轮审查 + 实机日志（`Background started FGS: Allowed [... uidState: RCVR ...]`）确认。
- **无障碍保活**：由系统回调驱动（规格要求实时更新、不得缓存为常量），附 10 秒兜底轮询应对直接 kill 进程的 ROM；
  健康状态**同时看两个信号**（通知权限 + 无障碍可用性），任一失效都显示降级文案。
- **服务假活防线**：前台循环整体加异常边界、`stopSelf()` 在循环退出后必达、连续失败写日志并停服务
  （否则表现为「状态显示监控中、实际一个 Tick 都不跑」）。

### 2.7 状态机修正：轮播（ROUND）按「未开播」处理

用户报告的严重问题：主播从「直播中」变成「轮播」时不被判定为下播 —— 场次不结束、下播通知不触发、
自动化记录全部失效。根因不是笔误，而是规格 0.6.23 当初**有意**让「轮播不结束场次」。
改动三处，语义一致：

| 位置 | 改动 |
|---|---|
| `MonitorRepository` | 把 ROUND 并入 OFFLINE 分支：关场次 + 写 `END` 事件 + 发下播通知 |
| `MonitoringEngine` | 在播数排除 ROUND（否则 `{live}` 会把轮播中的主播算作在播） |
| `StateConfirmationPolicy` | ROUND 从「LIVE 方向」挪到「OFFLINE 方向」，用下播阈值（默认 2 次）确认；**确认后的目标状态写回实际观察到的状态**，于是轮播仍记为 ROUND |

关键点是**判定语义与显示语义分离**：要不要关场次由仓储层决定，状态本身仍如实记为轮播。
两个把旧行为写死的单测同步更新（第二个测试的语义刻意保留：ROUND 仍是成功观察到的正常状态，不是异常）。

---

## 三、UI 设计体系

外观这一块的目标是「一套统一的视觉语言」，而不是给每个页面各写一套样式。

### 3.1 浮动卡片体系

| 组件 | 做法 |
|---|---|
| `FloatingTopBar` | 顶部标题栏改成浮起的圆角卡片（左右 12dp / 上下 8dp 留白、22dp 圆角 + 阴影、取 `surfaceContainer`）。参数与 `TopAppBar` 同名，13 处调用点只换函数名即可 |
| 标题居中 | 标题**两轴居中于整张卡片**，而不是在「左右按钮夹出来的剩余空间」里居中（后者只要右侧有按钮就看起来偏左）。左右留白按**两侧按钮的实测宽度**动态计算并设上限（按钮极多时标题至少保留可读宽度，不会被挤成 0 宽） |
| `HomeFilterCard` | 首页搜索框 + 状态/标签/分组筛选收进**一张可收起的卡片**；整行都是点击区，收起后仍显示筛选摘要；展开状态存 ViewModel（`rememberSaveable` 切 Tab 就丢） |
| `FloatingSearchField` | 历史一级/二级、统计一级三处搜索栏共用一个组件，形状/圆角/内边距由构造保证一致（此前是三段各自复制的 11 行，参数已开始漂移） |
| 自动继承外观 | 卡片颜色取主题 `surfaceContainer` 系列，因此标题栏与筛选卡**自动**继承卡片透明度/明暗与玻璃设置，不必单独写玻璃逻辑 |

### 3.2 Dock：从「bottomBar 槽位」到「浮层」

- Dock 原先放在 `Scaffold` 的 `bottomBar` 槽位里，**槽位会为它预留高度**，于是内容永远「停在 Dock 上方」；
  用户要的是内容能从 Dock **底下滚过去**。改法：Dock 移出槽位，作为叠在内容之上的浮层。
- 随之必须补的一步：内容不再自动避让，各页列表要自己留底部内边距。底部避让高度按 Dock 源码逐项核算
  为 **136dp**（26 栏体下沿留白 + 72 栏体 + 26 上沿留白 = 124，再加 12 视觉间隙），普通样式 92dp；
  首页的 FAB、消息条与列表末端留白共用同一常量（历史与统计一级页各自取同一数值），避免几处各写一个数。
- 两个只在设备上才能发现的细节：Dock 不会像 M3 `NavigationBar` 那样自己处理 windowInsets（第一版被导航键压住一截，
  补 `navigationBarsPadding()` 才对）；`Scaffold` 默认的 `contentWindowInsets` 底部会让列表永远停在导航键上方，
  要显式只保留顶部与左右（底部交给内容铺满，IME 另用 `imePadding` 补）。

### 3.3 液态玻璃：实现路径与**能力边界**

**先划边界**：Compose 没有「采样自己背后内容」的公开 API ——
`Modifier.blur()` 只模糊自己，`Window.setBackgroundBlurRadius()` 是整窗口生效；
Haze 那类通用解法要引入新依赖，而本项目构建以离线为主。

**Dock 的做法**：用 Compose UI 1.7 的 `rememberGraphicsLayer()` 每帧把整页内容录进图层，
再在全屏画布上按 **Dock 的实测矩形**裁剪、加 `RenderEffect` 模糊后 `drawLayer` ——
于是 Dock 里透出的是**它下面真实的内容**（包括滚动中的卡片），而不是一张静态壁纸。
之上再叠体色蒙版、顶部高光与 1dp 亮边，五项参数全部可自定义。
录制只在「Dock 样式 + 玻璃开启 + 底栏可见」时挂载（每帧全屏离屏渲染，代价是实打实的）。

**踩过的坑（三次，层层递进）**：

1. **内容根本没画进来**：把全屏图层画在 Dock 自己的小画布上再平移，整块被裁掉 ——
   当时连续两轮都在调蒙版浓度，等于在优化一个空图层。教训：**「没效果」时先确认「东西有没有画进来」，再调观感参数**。
2. **卡片不能消费同一图层**：卡片是被录制内容的**子孙节点**，让它 `drawLayer(这一层)` 形成自引用 ——
   实测 native 崩溃（crash buffer 里 `RenderNode::prepareTreeImpl` 与 `SkiaDisplayList::prepareListAndChildren` 交替数百帧，
   是渲染树无限递归的铁证）。Dock 能行，是因为它是被录制内容的**兄弟节点**。
   结论：**图层录制有方向性，消费者必须在被录制子树之外**。
3. **需求类比要追问**：用户说「卡片也要和 Dock 一样」，实现时按字面等价去做（复用实时层）撞上递归崩溃；
   而用户真正的验收标准是「透过卡片看到背后的壁纸」—— 壁纸一直就在卡片背后，只是被不透明的卡片挡住了。
   最终方案：卡片**半透明** + **壁纸磨砂**（磨砂放在背景层，全应用卡片一次性生效，
   代价是卡片没有自己的高光与亮边）。**注意"磨砂"不是无条件的**：磨砂半径就是「模糊半径」参数，
   用户把它设为 0 时壁纸并不磨砂，卡片只剩一层半透明底色 —— 所以对外文案不能写成
   "开启卡片玻璃就把壁纸整体磨砂"，只能写"磨砂程度由模糊参数决定"。
   如果当时问一句「你想透过卡片看到壁纸，还是看到滚动内容」，两轮就省下了。

| | 情况 |
|---|---|
| 能做到 | Dock 折射它下面**真实的内容**；参数可自定义；随时开关 |
| 做不到（已回退） | 卡片**实时折射**：需把「源」与「消费者」拆到两棵子树（Haze 的架构），涉及每页内容层级的结构重构，**未做** |
| 没设背景图时 | Dock 仍折射真实内容；「同时应用到所有卡片」没有壁纸可透（卡片只是变半透明），且与「模糊半径」取值无关 |
| 已知代价 | 每帧整页录制 = 每帧多画一遍全屏；若实测卡顿，优化方向是只录可见区域或加「实时/静态」开关（未做） |

### 3.4 外观体系的实现选择

- **卡片透明度/明暗改主题，而不是逐个页面改 `Card(colors=…)`**：M3 的容器色全部取自 `colorScheme`
  的 surface 系列，统一套上「透明度 + 压暗」即可全应用生效，也不会出现「某个页面漏改，看起来像 bug」。
- **主题色只派生 `primary` 与 `secondary` 两族**：M3 的 `FilterChip` 选中态取的是 `secondaryContainer`，
  只改 `primary` 会让横幅与选中的标签永远保持系统默认紫色（实测确认过）。全量 M3 色调表需要 HCT 色彩空间，
  收益只在「更协调」，不值得引入动态取色库。
- **吸管取的是背景图的颜色**，不是「屏幕任意位置」：后者需要无障碍截屏或录屏授权，为取一个颜色弹录屏框打扰大于收益；
  界面里写明了取色来源，未设背景图时按钮禁用。
- **滑块的通用策略**：拖动只改内存、松手才落库（否则会疯狂写 DataStore）。
- **覆盖同名文件的更新不能用路径做缓存键**：背景图换图是覆盖同一个文件，`remember(路径)` 永远命中旧值 ——
  第一次修成「路径 + 修改时间 + 大小」仍然无效，因为文件名不变、`AppearanceSettings` 新旧值 `equals` 相等、
  **组合根本没有重组**，key 表达式压根没被重新求值。最终引入会变的 `backgroundRevision`。
  教训：排查缓存类 bug，先确认「刷新链路通不通」，再看缓存键本身。

---

## 四、工程质量实践

### 4.1 单元测试：把「能算对」的部分独立出来

`domain/policy/` 与若干纯函数/纯对象是项目的「可测核心」，90 个用例分布如下：

| 测试 | 用例数 | 覆盖点 |
|---|---|---|
| `StateConfirmationPolicyTest` | 13 | 非成功观察不参与转换、首次观察、开播/关播确认阈值、失败不推进计数、方向切换计数复位、异常恢复、状态一致时复位、**ROUND 语义（按未开播确认）**、远端状态非法 |
| `RetryPolicyTest` | 10 | 总尝试次数与硬上限、指数退避与上限夹取、非法参数退化、熔断阈值/恢复窗口/成功清零 |
| `ParseStreamerInputTest` | 10 | 默认房间号、UID 模式、两类链接不受模式影响、超长数字不抛异常、零/空/乱码被拒 |
| `CropMathTest` | 9 | 图标裁剪的缩放/拖动换算：基准缩放、居中裁剪、越界夹取、退化输入不崩 |
| `ColorHexTest` | 8 | `#RGB`/`#RRGGBB`/`#AARRGGBB`、可选 `0x`、大小写、**空串=默认而非错误**、垃圾输入被拒、格式化往返 |
| `QuietHoursPolicyTest` | 8 | 跨午夜时段、起止相同视为未配置、边界包含判定 |
| `NotificationTemplateTest` | 8 | 占位符替换、重复占位符、未知数量显示 `—`、未知占位符原样保留、空白回落默认值、换行与长度规整 |
| `BackupChecksumTest` | 7 | **旧备份在 schema 加字段后仍能通过校验**、改动内容会被发现、清单缺失键的兼容比较 |
| `ExportNamingTest` | 7 | 历史/统计命名不同、多主播写法、无记录兜底、缺开播时间用关播兜底、非法字符替换、超长截断 |
| `SessionEndTruncationTest` | 6 | 断档截断纯函数：锚点必须是「最后一次成功观察」，失败观察不得推进 |
| `EnumSafeTest` | 4 | 未知枚举值退化为保守取值、合法值不受影响 |

instrumented 侧只有 1 个用例（`DiagnosticExporterTest`）：验证诊断包 6 个分片齐全、各分片是**合法 JSON**
（用真实解析器校验，而非字符串包含），并钉住两条硬性边界 —— **不含任何凭证**、**不含主播历史明细**。

### 4.2 缺陷台账制度

`docs/DEFECT_LEDGER.md` 是本项目最有价值的文件：**H1–H55** 每条都写了
「现象 / 根因 / 证据（文件:行 或 实测输出）/ 修法 / **验证状态**」，并显式标注**未验证与未做的部分**。

它带来的实际收益：

- 同类问题定位几乎是查表（例如「`runCatching` 吞异常」「清理顺序撞外键」「缓存键不含变更证据」都成组出现）；
- 把「走过的弯路」也记下来，避免重犯（例如观看历史 `-400` 的排查中，先怀疑 Referer / Cookie / WBI 签名 / 风控，
  真因其实是一个超限的参数 `ps=50`；教训是「面对『服务端只说参数错』，优先做输入边界的二分验证」）；
- 显式区分「已实现」「已编译」「已在设备验证」，所以文档不会把编译通过说成功能可用。

### 4.3 并行子代理审查与修复（四批实战）

| 批次 | 组织方式 | 结果 |
|---|---|---|
| 全项目静态审查 | 82 个 `.kt` / 18,518 行**纯静态阅读**，按关注点切五片（引擎/持久化/网络与凭证/UI/通知与核心）并行深读 | 跨切面 11 条 + 分片 74 条，去重约 60 条；四个系统性主题：线程归属无约定、失败可见性差（190 处 `runCatching`，87 处直接吞掉）、缺自愈路径、同一根因只修一半 |
| 第二轮验证审查 | 四个组并行，每组两件事：**逐条验证修复是否成立** + **搜出这些改动新引入的问题** | 「绝大多数修复成立」，但抓出 20 余条「改了一半 / 描述与实现不符 / 修复本身引入的新问题」（开机起不来实时监控、断档截断锚点用错、冷却时间闸仍有竞态…） |
| 数据层专项 | 只读审查代理用**真实 schema 建库 + 当时全量 228 条 `@Query` 逐条 EXPLAIN 预编译**验证 | 得出 **0 条 Room 编译期错误**，并实测复现出「清理仍会撞外键」这一遗留缺陷 |
| UI 全量审查 + 修复 | **3 个只读审查代理**按切片并行审查（约 70 条），**4 个写入代理**按**文件所有权互不重叠**并行修复 | 修复后编译通过、装机无崩溃；合并阶段暴露 1 处代理无法自证的编译错误（缺 `getValue/setValue` import） |

**做法上的几条经验**：

1. **审查与修复用两批代理**，避免「自己审自己」。
2. **子代理的推断再合理，也要用工具链事实判定**：审查代理报「三个文件因重复 import 编译不过」并称已复现，
   实际 `:app:compileDebugKotlin` 通过（K2 容忍完全相同的重复 import）—— 重复确实存在，但它是清理项，不是「编不过」。
3. **写入代理按文件所有权切分**，避免并发写同一文件；同时明确禁止代理跑 Gradle，把编译留给主流程统一做。
4. **代理的「偏离清单」要逐条判断而不是一律打回**：UI 批次里代理提出的 7 处偏离全部被采纳
   （例如底部留白用 136dp 而非清单里的 72dp、`glassCards` 改键名而非原地改类型以免老安装 `ClassCastException`）。
5. **合成验证仍然必要**：代理不跑编译，合并后才暴露真实的编译错误。

### 4.4 反复踩到的工程坑（与业务无关，但代价最大）

| 坑 | 教训 |
|---|---|
| 用 PowerShell `Get-Content -Raw` + `Set-Content -Encoding UTF8` 改一行代码，把中文按 ANSI 码页往返转换损坏（73 处字符丢失、两个字符串字面量断裂） | **改文件只用编辑工具**，不要用 shell 做读写往返 |
| 三次在「删/搬代码块」时踩到括号不平衡（把 `if` 的收尾花括号一起带走，编译报 `Expecting '}'`） | 行号 + 肉眼核对在长文件里不可靠；优先「整块抽到新文件 / 整块替换」，或用能识别语法边界的工具 |
| 批量替换时 `` `r`n `` 被当成字面量写进源码（编译报一串 `Unresolved reference 'r'`）；测试文件是 LF 行尾导致 CRLF 模式连续两轮匹配不上 | 涉及换行的批量改写用编辑工具；跨工具改文件前先确认行尾 |
| 用脚本批量插入开关时把它放进了 `values().forEach {}` 循环体内，界面上同一个开关渲染了两遍 | 批量插入后要检查落点，而不是只看编译是否通过 |
| `combine` 只有到 5 路的具名重载，第 6 路会静默退化到 `vararg` 重载（`Array<Any?>` + 手动转型） | 超过 5 路就嵌套拆分并写注释；项目里被这个坑绊过两次 |
| 为造测试数据手工插入了枚举里不存在的 `lifecycleState = 'CLOSED'`，应用反复崩溃 | 顺带证明了枚举列对未知值零容忍 → 促成 `EnumSafe` |
| 为定位 `-400` 曾把 HTTP 日志开到 `BODY` 级别，导致 `SESSDATA` 进入 logcat | 日志级别写死 `BASIC` 并在代码里写明原因；凭证永不落日志 |

---

## 五、已知局限与后续方向

### 已知局限

| 方向 | 现状 |
|---|---|
| 自动化测试 | 单测集中在纯函数；**迁移链（v1→v8）、部分唯一索引的实际约束力、恢复流程的幂等与失败回滚仍是零覆盖区**，需要 instrumented test 或 Robolectric |
| 卡片实时玻璃 | 需要在每页把「仅内容」录进图层、卡片与 Dock 都在该子树之外取景 —— 一次涉及各页内容层级的结构重构，未做 |
| 对话框 | `AlertDialog` 的 text 槽本身不滚动，横屏等极窄条件下可能被挤；彻底修需改对话框结构 |
| 导出 | 目前把整份 HTML 拼成字符串再写盘；超大历史库需要分批/流式改造 |
| 数据库损坏 | 只做「检测 + 上报」（`PRAGMA quick_check` + 健康事件），不做自动重建（自动重建意味着丢数据，需用户明确同意） |
| 外部依赖 | 全部数据来自 B 站公开接口：字段改名、参数上限、风控策略变化都会直接影响监控与导入（`ps` 硬上限 30、WBI 密钥轮换、`live_status` 字段缺失等都已实测记录在台账） |
| 主题色 | 不做深色模式下的对比度校验，极亮颜色在深色主题下可能对比不足 |
| 性能 | Dock 玻璃每帧录制整页（仅在需要时挂载）；封面下载做了超时与降采样，但大列表下的图片加载仍有优化空间 |
| 界面验证 | 若干观感类改动（历史二级页按钮间距、统计二级页 chip 换行、136dp 留白位置、大字体的对话框、a11y 播报）只做过编译验证，需要逐个在设备上看画面 |

### 如果继续做（按「危险程度 × 收益」排序）

1. **补 instrumented 测试**：`MigrationTestHelper` 从 v1 升到 v8、部分唯一索引的实际约束力、
   `sourceDataVersion` 并发递增、恢复的幂等与失败回滚 —— 这几块是「最危险的零覆盖区」。
2. **导出的分批/流式写入**：去掉「整份 HTML 拼字符串」的内存峰值。
3. **数据库损坏的自愈路径**：备份优先、用户确认后再重建，并与诊断包联动。
4. **从「每次全量轮询」改为「按主播热度自适应间隔」**：减少无效请求（而不是提高频率）。
5. **（可选）源/消费者子树分离**：把「仅列表内容」录进图层，让卡片与 Dock 都能在子树外取景 ——
   这是卡片实时玻璃唯一正确的实现路径，但需要逐页重构与逐页验证。
6. **静态/实时玻璃开关**：把「每帧整页录制」的取舍交给用户，低端机上优先流畅。
